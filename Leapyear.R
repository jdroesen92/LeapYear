#function to check whether a year is a leap year

leap_year  <- function(year) {
	if is.numeric(year)!=TRUE
	
		return("return data type is not correct")
	
		if ((year/4)%%0)!=TRUE)
		return("Common year")
				ifelse is.integer ((year/100)!=TRUE)
				return ("Leap year")
					if is.integer((year/400)!=TRUE)
					return ("Common year")
						else 
							return("Leap year")
	
}
test test

some minor change

